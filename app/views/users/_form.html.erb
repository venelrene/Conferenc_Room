<div class="container-fluid">

  <%= form_for(@user) do |f| %>
      <% if @user.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

            <ul>
              <% @user.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
              <% end %>
            </ul>
          </div>
      <% end %>
      <div class="field">
        <%= f.label :name %><br>
        <%= f.text_field :name %>
      </div><div class="field">
  <%= f.label :email %><br>
  <%= f.text_field :email %>
</div>
      <div class="field">
        <%= f.label :campus %><br>
        <%= f.text_field :campus %>
      </div class= "field">
      <%=radio_button_tag(:role, 'student')%>
      <%= label_tag(:student, 'I am a Student') %><br>
      <%=radio_button_tag(:role, 'teacher')%>
      <%= label_tag(:teacher, 'I am a Teacher') %>
      <div>

      </div>

      <div class="actions">
        <%= f.submit %>
      </div>
  <% end %>
</div>